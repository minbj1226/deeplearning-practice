# ==============================================
# pandas로 데이터 열어 보기 - "무슨 데이터인지"부터
# ==============================================

# 이 수업에서 계속 만날 파이썬 표현 7가지를 미리 본다.
# 1. CSV 파일을 pandas로 읽는다(한글 파일이라 encoding 주의)
# 2. 행 열 타입 요약통계를 본다
# 3. 결측 이상값 중복을 찾아 처리한다.
# 4. "이 데이터로 뭘 예측할 수 있나"를 정한다. -> 회귀 문제/분류 문제

import os  # 파일 경로 다루는 파이썬 기본 모듈
import pandas as pd  # 표(엑셀 같은 것)를 다루는 라이브러리
import numpy as np  # 숫자 배열을 빠르게 계산 하는 라이브러리

DATA = os.path.join(os.path.dirname(os.path.abspath(__file__)), "수업용데이터")
print(DATA)


def path(file_name):
    """deeplearning-practice/ 폴더 안의 파일 경로를 만들어 주는 작은 도우미 함수"""
    return os.path.join(DATA, file_name)


# # ===================================
# # 이 수업에서 계속 만날 파이썬 표현 7가지
# # ===================================

# x = 3.14159
# # x:.2f <- 소수 둘째 자리까지
# # x:8.3f <- 8칸 폭, 셋째자리
# # 7:>4d <- 정수를 4칸 오른쪽 정렬

# df = pd.read_csv(path("11_설비센서_ai4i.csv"), encoding="utf-8-sig")
# print(df)

# # ==============================================
# # 지저분한 데이터 다루기 - 12_제조센서_전처리.csv
# # ==============================================
# # 현실 데이터엔 반드시 구멍 존재. 일부로 구멍이 있는자 작은파알로 연습

# dirty = pd.read_csv(path("!2_제조센서_전처리.csv"), encoding="utf-8-sig")
# print("=" * 60)
# print("[5] 지저분한 데이터 원본")
# print(dirty)

# print()
# print("[5-1] 결측(Nan) 개수 - 열마다")
# print(dirty.isna().sum())

# # 결측 ? -> 값이 빠진 칸. 센서가 잠깐 먹통이었거나 입력을 안한 것

# print()
# print("[5-2] 이상값 - 물리적으로 말이 안되는 값 찾기")
# print("회전수가 음수인 행: ")
# print(dirty[dirty["회전수"] < 0])
# print("진동이 유난히 큰 행: ")
# print(dirty[dirty["진동"] > 10])

# # 이상값 = 다른 값들과 동떨어진 값, 무조건 지우는 게 아니다.
# # -50 rpm은 물리적으로 불가능 -> 센서 오류 -> 결측으로 취급
# # 진동 15.2는 상태 열에 '이상'이라 적혀 있으니 진짜 설비 이상일 수 있음 -> 남긴다.
# # 왜 지웠나 / 왜 남겼나를 주석으로 남기는 습관을 들인다. 무조건 프로젝트때 필수

# print()
# print("[5-3] 중복 행 - 같은 측정이 두 번 들어간 경우")
# print(
#     "중복 행 수: ", dirty.duplicated().sum()
# )  # duplicated() = 앞에 똑같은 행이 있었나 True/False
# print(dirty.duplicated(keep=False))  # keep = False -> 중복된 짝을 둘 다 보여줌

# # 결측 -> 그 열의 '중앙값'으로 매운다
# # 이상값 -> 물리적으로 불가능한 값 (음수 rpm)은 결측으로 바꾼 뒤 같이 메운다
# # 중복 -> 하나만 남긴다
# clean = dirty.copy()  # 원본은 건드리지 마라
# clean = dirty.drop_duplicates()  # 중복 제거
# clean.loc[clean["회전수"] < 0, "회전수"] = np.nan
# # 문법 .loc[조건, 열] = 조건에 맞는 행의 그 열 칸. 거기에 NaN을 넣음

# for 열 in ["온도", "압력", "회전수"]:
#     중앙값 = clean[열].median()
#     clean[열] = clean[열].fillna(중앙값)
#     print(f"{열}: 결측을 중앙값 {중앙값}로 채움")

# print()
# print("[5-4] 처리 후")
# print(clean)
# print("결측 남은 개수:", int(clean.isna().sum().sum()), "/ 행 수:", len(clean))

# # =======================================================
# # 이 데이터로 무엇을 예측할까 - 회귀 vs 분류
# # =======================================================

# # 같은 데이터(11_설비센서_ai4i.csv)로 두 종류의 질문을 던질 수 있다.
# # (a) 회귀 - 숫자를 맞춘다.
# # 질문: 공기온도가 이럴 때 공정온도는 몇 도임?
# # 입력 X = 공기온도 (나중엔 회전수 토크 공구 마모도 추가)
# # 정답 y = 공정온도 (304 ~ 317 사이의 연속된 숫자)
# # 왜 필요? 공정온도 센서가 없거나 고장 났을 때 다른 센서로 추정
# # 01 02 파일에서 numpy로 직접 함

# # (b) 분류 - 어느 편인지를 맞힌다
# # 질문: 이 설비, 고장이야? 아니야?
# # 입력 X = 공구마오, 토크, 회전수, 공기온도
# # 정답 y = 고장여부 (0 아니면 1, 둘 중 하나)
# # 왜 필요? 현장에선 "몇 도냐"보다 "멈춰야 하냐 아니냐"가 더 자주 필요한 질문
# # 03 파일에서 로지스틱 회귀로 푼다.

# # 판단 기준: 열이 '연속된 숫자'면 회귀, '몇 가지 중 하나'면 분류
# # (온도 개수 무게 가격 -> 회귀) (고장 양품 등급 합격 -> 분류)

# # =======================================================
# # 다음 파일로 넘기기 - X, y를 numpy 배열로 뽑는 법
# # =======================================================
# # pandas 표에서 .values를 붙이면 numpy 배열이 된다. 01부터는 이 배열로 계산함

# x = df["공기온도"].values
# y = df["공정온도"].values
# print(x.shape)
# print(y.shape)


# [문제] 12_제조센서_전처리.csv를 다시 읽어서, 진동이 3.2를 넘는 행만 골라 내기
# 몇 행인지와, 그 행들의 측정ID / 설비명 / 진동 / 상태를 출력
df = pd.read_csv(path("12_제조센서_전처리.csv"), encoding="utf-8-sig")
over_vib = df[df["진동"] > 3.2]

print(over_vib)
